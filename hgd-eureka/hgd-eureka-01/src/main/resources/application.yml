server:
  port: 9001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:http://${eureka.instance.hostname}:${service.port}/eureka/
  instance:
    hostname: 127.0.0.1

